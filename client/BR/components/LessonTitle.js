import React from 'react';
import ReactDOM from 'react-dom';

const LessonTitle = () => {
  return (
  <div style={{display: 'flex', justifyContent: 'center', alignContent: 'center', alignItems: 'cetner', backgroundColor: 'red', height: '100%', width: '100%'}}>
    <div>Beginning Readers Lesson 2: s & t</div>
  </div>
  );
};

export default LessonTitle;
